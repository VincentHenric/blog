<div class="row">
  <h1>Articles</h1>

  {{#each model as |article|}}
    <div class="article">
      <h2>{{article.title}}</h2>
      <h5>{{article.createdAt}}</h5>
      <p>{{article.text}}</p>
    </div>
  {{/each}}


</div>
